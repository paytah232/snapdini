server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # Serve the coming-soon page for every path; don't let it get indexed.
  add_header X-Robots-Tag "noindex" always;
  location / {
    try_files /index.html =404;
  }
}
